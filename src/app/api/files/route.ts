import {
  handlerRequest as GET,
  handlerRequest as PATCH,
  handlerRequest as DELETE,
  handlerRequest as POST,
} from '../../../../lib/api/files/handlerRequest';

export { GET, POST, PATCH, DELETE };
